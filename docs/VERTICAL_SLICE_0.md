# Vertical Slice 0 — historical note

This file described the first playable prototype. It is retained only as historical context and is no longer a design or technical authority.

Current authorities are, in order of responsibility:

- `GAME_DESIGN.md` for game-design rules;
- `ARCHITECTURE.md` for runtime architecture;
- `LOCALIZATION.md` for localization policy;
- TypeScript schemas for the executable content contract.

The current implementation uses Content Contract v2 and save v7. See `JAM_STATE.md` for its implemented scope.
